const ExampleResource = require("../resources/example.resource")

exports.ExampleCollection = (model) => {
    if (Array.isArray(model)) {
        const collection = model.map((resource) => (
            ExampleResource(resource)
        ))

        return collection
    }

    return []
}